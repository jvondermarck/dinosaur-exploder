name: Comment Artifact URL on PR

on:
  workflow_run:
    types:
      - completed
    workflows:
      - Upload JAR Artifact on PR

permissions:
  pull-requests: write
  actions: read

jobs:
  comment-on-success:
    if: github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    steps:

      - name: Event Obj Debug
        env:
          EVENT_OBJ: ${{ toJSON(github.event.workflow_run) }}
        run: |
          jq '.' <<< "$EVENT_OBJ"
          jq '.pull_requests' <<< "$EVENT_OBJ"

      - name: Get Artifact URL & PR Info
        env:
          GITHUB_TOKEN: ${{ github.token }}
          OWNER: ${{ github.repository_owner }}
          REPO: ${{ github.event.repository.name }}
          WORKFLOW_RUN_EVENT_OBJ: ${{ toJSON(github.event.workflow_run) }}
        run: |
          PREVIOUS_JOB_ID=$(jq -r '.id' <<< "$WORKFLOW_RUN_EVENT_OBJ")
          echo "PREVIOUS_JOB_ID=$PREVIOUS_JOB_ID" >> "$GITHUB_ENV"

          # Get artifact ID
          ARTIFACT_ID=$(gh api "/repos/$OWNER/$REPO/actions/artifacts" \
            --jq ".artifacts.[] |
            select(.workflow_run.id == ${PREVIOUS_JOB_ID}) |
            select(.expired == false) |
            .id")
          echo "ARTIFACT_ID=$ARTIFACT_ID" >> "$GITHUB_ENV"

          # Human-friendly artifact URL
          ARTIFACT_HUMAN_URL="https://github.com/$OWNER/$REPO/actions/runs/$PREVIOUS_JOB_ID/artifacts/$ARTIFACT_ID"
          echo "ARTIFACT_HUMAN_URL=$ARTIFACT_HUMAN_URL" >> "$GITHUB_ENV"

          PR_NUMBER=$(jq -r '.pull_requests[0].number' <<< "$WORKFLOW_RUN_EVENT_OBJ")
          echo "PR_NUMBER=$PR_NUMBER" >> "$GITHUB_ENV"

          HEAD_SHA=$(jq -r '.pull_requests[0].head.sha' <<< "$WORKFLOW_RUN_EVENT_OBJ")
          echo "HEAD_SHA=$HEAD_SHA" >> "$GITHUB_ENV"

      - name: Find previous comment
        id: find-comment
        uses: peter-evans/find-comment@v3
        with:
          issue-number: ${{ env.PR_NUMBER }}
          comment-author: 'github-actions[bot]'
          body-includes: '<!-- build-preview-comment -->'

      - name: Update Comment
        env:
          JOB_PATH: "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ env.PREVIOUS_JOB_ID }}"
          HEAD_SHA: ${{ env.HEAD_SHA }}
          ARTIFACT_HUMAN_URL: ${{ env.ARTIFACT_HUMAN_URL }}
        uses: peter-evans/create-or-update-comment@v2
        with:
          issue-number: ${{ env.PR_NUMBER }}
          comment-id: ${{ steps.find-comment.outputs.comment-id }}
          edit-mode: replace
          body: |-
            <!-- build-preview-comment -->
            
            ## Build preview 🚀
            [![badge]](${{ env.JOB_PATH }})
            
            **Thanks** for your **contribution** to this open-source project! ❤️
            
            A **new build** has been generated for this pull request:
            
            - 📦 **Artifact**: [Download JAR](${{ env.ARTIFACT_HUMAN_URL }})
            - 🧪 **Workflow Run**: [View Logs](${{ env.JOB_PATH }})
            - 🔖 **Commit**: `${{ env.HEAD_SHA }}`
            
            > [!TIP]
            > This JAR lets you test the latest version of the game from this PR.
            
            ⏳ **Note**: Artifacts auto-expire after ~90 days.
            
            Happy testing & enjoy the game! 🎮
            
            _The DinoBot Team_ 🦖
            
            [badge]: https://img.shields.io/badge/Build-Success!-3fb950?logo=github&style=for-the-badge

  comment-on-failure:
    if: github.event.workflow_run.conclusion == 'failure'
    runs-on: ubuntu-latest
    steps:
      - name: Get PR Info
        env:
          WORKFLOW_RUN_EVENT_OBJ: ${{ toJSON(github.event.workflow_run) }}
        run: |
          PR_NUMBER=$(jq -r '.pull_requests[0].number' <<< "$WORKFLOW_RUN_EVENT_OBJ")
          HEAD_SHA=$(jq -r '.pull_requests[0].head.sha' <<< "$WORKFLOW_RUN_EVENT_OBJ")
          echo "PR_NUMBER=$PR_NUMBER" >> "$GITHUB_ENV"
          echo "HEAD_SHA=$HEAD_SHA" >> "$GITHUB_ENV"

      - name: Find previous comment
        id: find-comment
        uses: peter-evans/find-comment@v3
        with:
          issue-number: ${{ env.PR_NUMBER }}
          comment-author: 'github-actions[bot]'
          body-includes: '<!-- build-preview-comment -->'

      - name: Leave failure comment
        uses: peter-evans/create-or-update-comment@v2
        with:
          issue-number: ${{ env.PR_NUMBER }}
          comment-id: ${{ steps.find-comment.outputs.comment-id }}
          edit-mode: replace
          body: |-
            <!-- build-preview-comment -->

            ## ❌ Build Failed

            [![badge-fail]](${{
              github.server_url }}/${{ github.repository }}/actions/runs/${{ github.event.workflow_run.id }})

            **Thanks** for your **contribution** to this open-source project! ❤️
            
            Unfortunately the build for this pull request has **failed**:
            
            - 🚩 **Workflow Run**: [View Logs](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.event.workflow_run.id }})
            - 🔖 **Commit**: `${{ env.HEAD_SHA }}`
            
            > [!TIP]
            > Please check the logs and fix any issues before re-running the workflow.
            
            Good luck with your debugging! 🐛 (or should I say _dino-bugging_? 🦖)

            _The DinoBot Team_ 🦖

            [badge-fail]: https://img.shields.io/badge/Build-Failed-red?logo=github&style=for-the-badge